# Phi-nance Dashboard — beta/VPS defaults
# Binds to all interfaces so the app is reachable from other machines.

[server]
address = "0.0.0.0"
port = 8501
enableCORS = false
enableXsrfProtection = false

[browser]
gatherUsageStats = false
serverAddress = "165.245.142.100"
