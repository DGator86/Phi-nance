# Phi-nance — Live Backtest Workbench
# Dark mode only. Purple + orange accents.

[server]
address = "0.0.0.0"
port = 8501
enableCORS = false
enableXsrfProtection = false

[browser]
gatherUsageStats = false

[theme]
base = "dark"
primaryColor = "#a855f7"
backgroundColor = "#0f0f12"
secondaryBackgroundColor = "#1a1a1f"
textColor = "#e4e4e7"
font = "sans serif"
