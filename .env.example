# Copy to .env and fill in. Never commit .env.
# Rotate any keys that were ever pasted into chat or logs.
#
# Phase 2: run  python -m scripts.run_phase2
# If backfill fails with 401/403: key is wrong or expired; set correct key here.

# Tradier (live bars + options chain). Sandbox: https://sandbox.tradier.com
TRADIER_ACCESS_TOKEN=

# Polygon or Massive (historical 1m bars, 2+ years for WF). One of these required for backfill.
POLYGON_API_KEY=
# MASSIVE_API_KEY=

# FINRA short volume: no key (we use public CDN).
