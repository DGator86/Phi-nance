lumibot==4.4.52
headroom-ai>=0.3.7
anthropic>=0.40.0
streamlit>=1.30.0
plotly>=5.18
pyyaml>=6.0
numpy>=1.24
pandas>=2.0
pydantic>=2.0
requests>=2.28
python-dotenv>=1.0
# Phase 3: Polygon.io live L2 feed (>=1.16: websockets>=14, compatible with yfinance)
polygon-api-client>=1.16.0
websocket-client>=1.7
sortedcontainers>=2.4
# Phase 2+: options analytics
scipy>=1.11
numba>=0.59

# Data acquisition (for generate_training_data.py)
yfinance>=0.2.40

# ML Frameworks (Tier 1 — required for ML strategies)
scikit-learn>=1.3.0
hmmlearn>=0.3.2
lightgbm>=4.0.0
joblib>=1.3.0

# RL (Tier 2 — optional, only needed for RLStrategy + train_rl_agent.py)
# stable-baselines3>=2.0.0
# gymnasium>=0.28.0

# Plutus Bot — LLM trading via Ollama (requests already listed above)
# Ollama itself is a standalone binary, not a pip package.
# Install: https://ollama.com/download
# Then:    ollama pull 0xroyce/plutus
# The PlutusAdvisor communicates with Ollama via its REST API (requests>=2.28).

alpaca-py>=0.20.0
fredapi>=0.5.2
# thetadata>=0.9.11  # yanked from PyPI; install manually if needed
cachetools>=5.3.3
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
financialmodelingprep>=0.0.2

# Phase 4: genetic programming
deap>=1.4.1

# Ray — NOT on default install: often missing wheels (e.g. Windows + Python 3.13).
# Distributed backtests / RayEnvRunner degrade without it. Linux/macOS with a
# supported Python:  pip install -r requirements-ray.txt
#
# ray[default]>=2.30.0

# Experiment tracking
mlflow>=2.14.0
# Optional backend
wandb>=0.17.0

matplotlib>=3.8
jupyter>=1.0.0
ipykernel>=6.29
fasteners>=0.19
tenacity>=8.2.3
pandera>=0.20.0
optuna>=3.6.0

# Legacy REST client for phi.live.broker.AlpacaBroker only — pins websockets<11 and
# conflicts with polygon-api-client>=1.16. Prefer alpaca-py (above). If you need it:
#   pip install -r requirements-legacy-alpaca.txt   # may force-downgrade websockets; not recommended with Polygon.

# Deep regime detection
torch>=2.2.0
tqdm>=4.66

statsmodels>=0.14.0

scikit-optimize>=0.9.0
